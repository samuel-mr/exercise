   // let frase = "hola como estas";
    // let result = frase
    //     .split(" ")
    //     .map(|x| x.as_bytes().iter().map(|&y| (y - 96) as u16).sum::<u16>());

    // let result2 = frase2.split(" ");

    // let max = result.max().unwrap();
    // let max2 = result2.max().unwrap();
    // //let first = result.filter(|&x| x == max).next();
    // println!("{:?}", result2);